<%= form_for note do |f| %>
    <%= f.label :book_id %>
    <%= f.select :book_id, options_for_select(books) %>
<% end %>

<% if false %>

, options_for_select(article_types, article.type), include_blank:true %>

<% end %>
